# Production: point to your API server
VITE_API_URL=https://fazistore-api.fazilabs.com

# Production: public base URL for MinIO-stored images
# Nginx on the API server proxies /fazipos/* to MinIO internally
VITE_MINIO_PUBLIC_URL=https://fazistore-api.fazilabs.com/
